apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pinepods-backups-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: openebs-hostpath
