apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pinepods-valkey-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: openebs-hostpath
