apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: photoprism-data-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: openebs-hostpath
