apiVersion: v1
kind: Service
metadata:
  name: cloudflare-gateway-profile
spec:
  selector:
    app: cloudflare-gateway-profile
  ports:
    - protocol: TCP
      port: 80
      # The pod listens on 8080 because it runs unprivileged; see deployment.yaml.
      targetPort: http
